node_modules/
.expo/
dist/
.export-check/
android/
ios/
*.apk
*.aab
.env
.env.local

# Firebase credential — see app.config.js's docblock for why the build tolerates its absence.
google-services.json
